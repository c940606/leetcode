class Solution:
    def maximalRectangle(self, matrix):

        row = len(matrix)
        col = len(matrix[0])

        for i in range(row):
            left = 0
            right = 0
            up = 0
            down  = 0
            for j, k in zip(range())

